const DATABASE_NAME = "@gameplay";

// @app:colecao

const COLLECTION_USERS = `${DATABASE_NAME}:user`;
const COLLECTION_APPOINTMENTTS = `${DATABASE_NAME}:appointments`;

export { COLLECTION_USERS, COLLECTION_APPOINTMENTTS };
